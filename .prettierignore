node_modules
dist
coverage

# Playwright 산출물
test-results
playwright-report
blob-report
tests/__screenshots__

# 약관 3종은 syncrun 허브 `legal/`의 사본이다 — 원문과 바이트로 같아야 `sync.sh` 대조가 성립한다.
# 이 레포에서 본문을 고치지 않으므로 포맷터도 손대지 않는다.
src/legal/docs

# 카피 테이블은 코드가 아니라 콘텐츠다. 한국어 산문은 하나의 문자열 리터럴이라 포맷터가 줄바꿈할 수
# 없고, 손으로 잡아둔 배치(섹션당 한 줄, 키 아래 본문)가 훑어보기에 낫다. 포맷터가 얻을 것이 없다.
src/i18n
